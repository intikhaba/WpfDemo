<Window x:Class="WpfDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:uc="clr-namespace:WpfDemo.UserControls"
        xmlns:local="clr-namespace:WpfDemo"
        xmlns:viewmodel="clr-namespace:WpfDemo.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodel:CustomerMainViewModel, IsDesignTimeCreatable=True}"
        Title="Customer" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <uc:CustomerEntry Grid.Row="0" Grid.Column="0" DataContext="{Binding CustomerEntryViewModel}" />
        <uc:CustomerList Grid.Row="0" Grid.Column="1" DataContext="{Binding CustomerListViewModel}" />
    </Grid>
</Window>
